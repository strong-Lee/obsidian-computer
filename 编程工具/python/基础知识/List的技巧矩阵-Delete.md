#### 1. ⚡ 极速：尾部弹出 (Pop Last)

**场景**：移除最后一个元素（栈操作）。  
**PHP思维**：array_pop($arr)。
```python
stack = [10, 20, 30]

# ⚡ 极速 (Instant/Atomic) - O(1)
val = stack.pop() # 返回 30

# 💡 源码级剖析 (Source Code Analysis)
# --------------------------------------------------------------------
# 1. 简单调整:
#    - `ob_size--`。
#    - 没有任何内存移动。
#    - 可能会触发 `realloc` 缩小内存（但 Python 策略非常保守，通常懒得缩小，保留空间给未来用）。
#    - 绝对推荐。
```

#### 2. 💀 性能杀手：头部/中间弹出 (Pop Index / Remove)

**场景**：移除第一个元素（队列操作），或移除特定值的元素。  
**PHP思维**：array_shift (PHP 链表操作很快，但在 Python 是灾难)。
```python
queue = [10, 20, 30, 40, 50]

# 💀 性能杀手 (Killer) - O(N)
# 动作：移除索引 0
val = queue.pop(0)

# 💀 性能杀手 (Killer) - O(N)
# 动作：移除值为 30 的元素 (即使它在中间)
# queue.remove(30)

# 💡 源码级剖析 (Source Code Analysis)
# --------------------------------------------------------------------
# 1. Pop(0) 原理:
#    - 必须把索引 1 到 N-1 的所有元素前移一位。
#    - `memmove` 再次登场。
#
# 2. Remove(val) 原理:
#    - 必须先遍历找到 val 的位置 (查找 O(N))。
#    - 然后再把后面的元素移过来 (搬运 O(N))。
#    - 如果循环 `while val in list: list.remove(val)`，这就是经典的 **O(N^2)** 算法，数据量一上万就卡死。
#
# 3. 架构优化:
#    - 碰到这种需求，请立刻换用 `collections.deque` 或 `set`。
```

#### 3. 🚀 推荐：切片删除 (Del Slice)

**场景**：删除一部分元素，比如保留前 2 个，删除后面所有的。  
**PHP思维**：array_splice。
```python
data = [1, 2, 3, 4, 5]

# 🚀 推荐 (Fast/Pythonic) - O(K) K=移动数量
del data[2:] # 删除索引 2 之后的所有

# 💡 源码级剖析 (Source Code Analysis)
# --------------------------------------------------------------------
# 1. 智能处理:
#    - 如果是切掉尾巴，本质上就是调整 `ob_size`，非常快。
#    - 如果是切掉中间 `del data[1:3]`，则需要把后面的搬运回来。
```

#### 4. ⚡ 极速：清空列表 (Clear)

**场景**：删除所有元素。  
**PHP思维**：$arr = []。
```python
data = [1, 2, 3]
another_ref = data

# ⚡ 极速 (Instant/Atomic) - O(1)
data.clear()

# 结果：data 和 another_ref 都变成了 []。

# ❌ 错误做法
# data = [] 
# 这只是把 data 标签指向了一个新列表。another_ref 还是指向旧数据！

# 💡 源码级剖析 (Source Code Analysis)
# --------------------------------------------------------------------
# 1. 引用计数风暴:
#    - `clear()` 会把列表里所有元素的引用计数减 1。
#    - `ob_size` 设为 0。
#    - 内存不会立即释放回操作系统，而是留着下次复用。
```

#### 5. 🏴‍☠️ 黑客：异常安全的弹出 (Exception-Safe Pop)

**场景**：从列表弹出元素，但列表可能为空。  
**PHP思维**：array_pop 如果空返回 NULL。  
**Python思维**：pop() 如果空抛出 IndexError。
```python
stack = []

# 🚀 推荐 (Fast/Pythonic) - LBYL (Look Before You Leap)
if stack:
    val = stack.pop()
else:
    val = None

# 或者 EAFP (Easier to Ask for Forgiveness than Permission)
# try:
#     val = stack.pop()
# except IndexError:
#     val = None

# 💡 源码级剖析 (Source Code Analysis)
# --------------------------------------------------------------------
# 1. 布尔值判定:
#    - `if stack:` 会调用 `PyList_Size`。
#    - 这是一个 O(1) 的检查。
#    - 比 try-except 块（涉及设置异常处理栈帧）要轻量那么一点点（在非异常情况下几乎没差，但在空列表频繁时 if 更快）。
#
# 2. 线程安全提示:
#    - 如果是多线程环境，`if stack:` 和 `stack.pop()` 之间可能被插队！
#    - 此时必须用 try-except 或者加锁。
```

#### 6. 🚀 进阶：使用 del 删除变量引用 vs 清空列表

**场景**：你想释放内存。  
**PHP思维**：unset($arr)。
```python
big_list = [1] * 1000000

# 动作 A: 清空内容
# big_list.clear() 
# 结果：big_list 变为空列表，原内存块可能被释放或保留（取决于 Python 内存池），对象依然存在。

# 动作 B: 删除引用
del big_list
# 结果：变量名 `big_list` 从当前命名空间移除。
# 引用计数：原列表对象的引用计数 -1。
# 如果计数归零 -> 触发 GC -> 释放内存。

# 💡 源码级剖析 (Source Code Analysis)
# --------------------------------------------------------------------
# 1. 区别：
#    - `clear()` 是修改对象的内容。
#    - `del var` 是删除变量名。
#    - 如果你有 `a = big_list`，`del big_list` 后 `a` 依然有数据。`big_list.clear()` 后 `a` 也就空了。
```

#### 7. 💀 性能杀手：反复的 min / max 查找并删除

**场景**：实现一个简单的优先队列（每次取最小的并删除）。  
**PHP思维**：min($arr) 找到值，然后 array_search 找 key，然后 unset。
```python
data = [5, 1, 9, 3]

# 💀 性能杀手 (Killer) - O(N^2) 整体
# while data:
#     m = min(data)      # O(N) 扫描
#     data.remove(m)     # O(N) 扫描找位置 + O(N) 搬运

# 🚀 推荐 (Fast/Pythonic) - Heapq
import heapq
heapq.heapify(data)      # O(N) 建堆
while data:
    m = heapq.heappop(data) # O(log N)
    # 处理 m

# 💡 源码级剖析 (Source Code Analysis)
# --------------------------------------------------------------------
# 1. Heapq 原理:
#    - `heapq` 模块直接在 List 的内存布局上维护堆属性（二叉堆）。
#    - `heappop` 会把 list[0] 拿出来，把 list[-1] 放到 0，然后执行 sift-down 下沉操作。
#    - 避免了 `memmove` 大量移动，只进行少量的 swap。
#
# 2. 架构思维:
#    - 任何涉及“频繁查找最值并删除”的场景，List 都是错误的工具，必须用 Heap。
```

#### 8. ⚡ 极速：利用 Sentinel (哨兵) 清理数据

**场景**：你需要在一个循环中不断读取并处理数据，直到遇到特定的停止符号。  
**PHP思维**：while 循环。
```python
# 假设 source.read() 每次返回一个数据，结束时返回 ''
# 🚀 推荐 (Fast/Pythonic) - iter(callable, sentinel)
blocks = []
for block in iter(source.read, ''):
    blocks.append(block)

# 💡 源码级剖析 (Source Code Analysis)
# --------------------------------------------------------------------
# 1. iter 的二元形式:
#    - 这是 CPython 的一个隐藏功能。
#    - 它会在 C 循环中不断调用 `source.read()`。
#    - 每次调用后，立即在 C 层面检查返回值是否等于 `''` (Sentinel)。
#    - 如果不等，yield 出来；如果相等，StopIteration。
#
# 2. 优势：
#    - 比写 `while True: data = read(); if data == '': break; ...` 要快，
#      因为省去了 Python 字节码层面的多次比较和跳转指令。
```