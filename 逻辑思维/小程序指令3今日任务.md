```json
《今日任务 (Today's Task)》
任务名称:
重构“今日课表”页面 (`today-schedule`)
任务描述:
请根据《核心指令集》和《应用设计蓝图》中的定义，为我彻底重构“今日课表”页面。
具体要求:
1. 信息架构: 必须实现《应用设计蓝图》中定义的“分段控件”用于日/周/月视图切换，并包含日期导航和课程列表。 
2. 交互逻辑: 页面需要能够处理数据加载 (`isLoading`) 状态，并为课程点击事件提供 `handleCourseClick` 方法的占位。 
3. 视觉实现: 严格使用《核心指令集》中定义的设计系统 `tokens` 和 `CSS` 变量/类。 
4. 架构遵从: 所有API请求必须使用 `this.$api.http`，路由跳转使用 `this.$api.router`，弹窗使用 `this.$refs.dialogs`。 
待重构的旧代码 (如果需要参考):
。。。
```
